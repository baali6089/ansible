- name: default
  hosts: web
  tasks:
  - name: default value
    ansible.builtin.debug:
      msg: "hello {{course | default('ansible')}}"

- name: upper case
  hosts: web
  vars:
    greeting: good moring
  - name: upper case
    ansible.buildin.debug:
      msg: "{{greeting | upper }}"        